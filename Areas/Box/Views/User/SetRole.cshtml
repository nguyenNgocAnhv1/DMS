@model App.Models.BoxShareList
@{
     var list = ViewBag.listAllRole as MultiSelectList;
     var id = ViewBag.id;
}
@Html.Partial("_ThongBao")
<h3>Them role cho user @ViewBag.name</h3>
<div class="row">
     <div class="col-6">
          <form asp-action="SetRole" asp-controller="User" asp-route-id="@id">
               <div class="form-group">
                    <label asp-for="RoleList" class="control-label"></label>
                    <select class="w-50" asp-for="RoleList" multiple="multiple" asp-items="list"
                         class="form-control"></select>
                    <span asp-validation-for="RoleList" class="text-danger"></span>
               </div>
               <div class="form-group">
                    <input type="submit" value="Update" class="btn btn-primary" />
               </div>
          </form>
     </div>
</div>
<a asp-action="MyAdmin" asp-controller="user">Quay ve</a>